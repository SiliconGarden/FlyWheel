# FlyWheel Python dependencies (Python 3.9).
#
# Not installable via pip — must exist separately:
#   - ffmpeg + ffprobe on PATH
#   - "League Spartan" font installed on the system
#
# Versions below are the known-good set from the working environment
# (macOS system python3 3.9 + user site-packages).
#
# To recreate the environment OUTSIDE any cloud-synced folder (OneDrive/Dropbox):
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

openai-whisper==20240930
torch==2.8.0
numpy==2.0.2
opencv-python==4.13.0.92
mediapipe==0.10.33
pillow==11.3.0
